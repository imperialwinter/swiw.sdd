flagDefs = {
	neutralFlag = 'a_p_flag',	--The internal name of the unit the spawner will use.
	defaultFeature = nil		--The name of the feature to spawn with the neutral unit
					--if no map profile is present.
}

return flagDefs